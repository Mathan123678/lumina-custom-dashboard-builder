PORT=5000
# Optional: if unset/unreachable, server falls back to JSON file persistence
# MONGODB_URI=mongodb://localhost:27017/telecom_dashboard

# Optional: defaults to 'secretkey' if unset (set this in real deployments)
JWT_SECRET=secretkey

